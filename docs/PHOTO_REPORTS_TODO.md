Фотоотчеты — TODO план и внедрения

1) Группировка и категории
- Добавить поля `tags[]`, `stage` к фотоотчетам (клиентские метаданные)
- Фильтр по тегам (чипы), групповка по `stage`
- Хронологическая лента по `date` (сортировка по убыванию)

2) Расширенное редактирование
- DnD-перетаскивание фотографий в редактировании фотоотчета
- Массовое удаление (план)

3) Производительность
- Ленивая загрузка изображений (`LazyImage`, IntersectionObserver)
- Виртуализированная сетка для больших списков (`VirtualizedPhotoGrid`)

4) Кеширование
- Кеш миниатюр в IndexedDB (`thumbnailCache`), прогрессивная загрузка
- Предзагрузка соседних фото в просмотрщике (next/prev)
- Офлайн: локальные метаданные фотоотчетов (tags/stage) + миниатюры

5) Оптимизация изображений
- Миниатюры в WebP с fallback на JPEG (через canvas)
- Адаптивные (малые) превью для списка; оригинал — в просмотрщике

6) Touch-интерфейс
- Свайп навигация (влево/вправо)
- Pinch-to-zoom (масштаб x1–x4)
- Долгое нажатие — контекстные действия (удалить/закрыть)

7) Адаптивная сетка
- `.photo-grid { grid-template-columns: repeat(auto-fit, minmax(120px, 1fr)); gap: 8px }`
- `@media (max-width: 768px) { .photo-grid { grid-template-columns: repeat(3, 1fr); gap: 4px } }`

Статус выполненного (в этой ветке):
- [x] Поля tags/stage (клиентские), UI добавления/редактирования
- [x] Фильтрация по тегам, группировка по этапам
- [x] DnD-перетаскивание порядка фото в редакторе
- [x] LazyImage + прогрессивные миниатюры из IndexedDB
- [x] Предзагрузка соседних фото в просмотрщике
- [x] Touch: свайп, pinch-to-zoom, long-press меню
- [x] Обновление CSS сетки под макет
- [x] Интеграция VirtualizedPhotoGrid в просмотрщик при >80 фото
- [x] Селектор этапов из WorkStage в модалках создания/редактирования
- [x] PWA SW: кэш оригиналов с Supabase (внешний origin), фоновые обновления

Дальнейшие улучшения (по желанию):
- [ ] Заменить мини-ленты на `VirtualizedPhotoGrid` в отдельных местах с очень большими коллекциями
- [ ] Сервис-воркер для офлайн картинок и синхронизации (PWA)
- [ ] Привязка `stage` к реальным этапам проекта (WorkStage) через селектор
- [ ] Массовое редактирование тегов/этапов

Примечания по внедрению
- VirtualizedPhotoGrid активируется в `PhotoViewerModal` при количестве фото > 80, высота контейнера 260px, размер ячейки 80px.
- Селектор этапа строится из `workStages` проекта. В `PhotoReportModal` и `EditPhotoReportModal` при наличии этапов отображается `<select>`, иначе — текстовое поле.
- SW (public/sw.js) теперь кэширует также внешние изображения (включая Supabase Storage). Стратегии: `Cache First` для изображений, `Stale-While-Revalidate` для остальных.
